package main

import "testing"


func TestminimumBribes(t *testing.T) {
	result := minimumBribes(arr, d)
	if result != answer) {
		t.Errorf("minimumBribes was incorrect, got: %v, want: %v.", result, answer)
	}
}
